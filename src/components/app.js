import React, { Component } from 'react';
import CalculatorContainer from './container-calculator';

export default class App extends Component {
    render() {
        return (
            <div>
                <CalculatorContainer />
            </div>
        );
  }
}
